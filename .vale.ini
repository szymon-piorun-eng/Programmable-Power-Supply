StylesPath = ./.github/styles
MinAlertLevel = suggestion

Vocab = Base

Packages = Microsoft, Google

[*.md]
BasedOnStyles = Vale, Microsoft, Google

TokenIgnores = (\$.*?\$), (\{:.*?\}), (\[TBD\]), ({{.*?}})

BlockIgnores = (?m)^(\s*!!! [a-z]+ ".*?")$

Microsoft.Passive = NO

Google.Parens = NO

